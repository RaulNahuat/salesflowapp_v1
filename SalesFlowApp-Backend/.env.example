# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MySQL)
DB_HOST=your_db_host
DB_PORT=16862
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=salesflow

# JWT Configuration
# 🔒 SECURITY: Generar un secreto fuerte con:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# IMPORTANTE: Mínimo 32 caracteres, recomendado 64+
# El servidor NO iniciará si el secreto tiene menos de 32 caracteres
JWT_SECRET=your_jwt_secret_key_minimum_32_characters_recommended_64_or_more
JWT_EXPIRE=30d

# CORS Configuration
FRONTEND_URL=http://localhost:5173
